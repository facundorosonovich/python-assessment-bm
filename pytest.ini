[pytest]
# Specify the browser type (chromium, firefox, webkit)
playwright_browser_type = chromium

# Optionally specify the browser executable path
# playwright_browser_executable_path = /path/to/browser/executable